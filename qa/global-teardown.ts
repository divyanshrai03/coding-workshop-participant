/**
 * Runs once after the whole suite. Placeholder for future cleanup (e.g.
 * deleting data created by test runs) once Phase 3/4 fixtures start creating
 * it. Intentionally empty for now - no feature-specific behavior yet.
 */
export default async function globalTeardown(): Promise<void> {
  // Nothing to clean up yet.
}
